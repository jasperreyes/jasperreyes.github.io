// get browser width
// get browser height

// calculate amount of rows based on browser height
	// browser height / tile height = var rowsPerBrowser
	$(window).height();

// loop repeat
	//if the number of tiles in a row equals tilesPerRow, start a new row
	//if the number of rows looped equals rowsPerBrowser, end loop


// calculate amount of tiles per row
	// browser width / tile width = var tilesPerRow
	$(window).width();
